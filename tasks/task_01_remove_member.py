"""Task 1: Remove a member safely.

Add a remove_member(member_id) method to library/library.py.

Requirements:
- Reuse find_member().
- If the member still has borrowed books, raise ValueError.
- Otherwise remove the member from self.members.
"""
